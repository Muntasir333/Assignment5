1. Var means variable. its an old way. And we don't use it because it hoisted.Function-scoped. It can be redeclared and redesigned.
let is block scoped. can be re assigned. we use it when we have to change variable.
const is also block scoped and it can't be reassigned. We will use Let Always.
2. Spread operator usually spread an array or object to individual element. Used to merge, copy and pass multiple values in arry, object. done by(...).
3. Map()- transform each elements and give us a new array.
Filter()-Selects elements and filter in an array and return us a new array.
for Each()- It is a looping concept like for in/off. It just loops over Elements and give us single entity.
4. Its a shorter sway to write function. easy to use. Uses =>.
const add=(a,b)=> a+b;
5. template literals are (``). We use it when we try change something dynamically. we also use ${} to function it properly.